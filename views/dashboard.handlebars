<!-- Loop over each object in the projects array -->
{{#each userResults as |results|}}
<details>

    <summary>
        <a href="/dashboard/{{results.id}}">{{results.title}}</a>
        {{!-- <span class="icon">👇</span> --}}
    </summary>
    <p>{{results.content}}</p>

</details>
{{/each}}
<button onclick="window.location.href='/blog'">New Blog Post</button>
